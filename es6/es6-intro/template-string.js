const priya = 'Asif AKbar';
const meye = 'Meye tumi ki dukkho chino';
const kobita = `kobita tumi sopno charini`;


// `` <==== Backtik



const multiLine = `this is my first line 
this is my second 
    Heelo
    hi
    hello
    hi
    this is multi line string using carret and using sooooooo many things
`;

console.log(multiLine);


// ` to use multiline string and aviod + we need to use  
//(` `);

const count = 5;
const oldHeader = `
    <h3 class = "friend-name"> Friend </h3>
`;

const new1 = `<h3 class = "friend-name"> Friend-${count} </h3>`;



const friends = ['abul','kabul','chabiul'];


const frist = 'Mamun';

const last = 'chowdhury';

const fullName = `${frist} ${last}`;

const fullNew = ` This person name is : ${frist} ${last} . Has money ${friends.length * 500}.He lives in Dhaka`;

console.log(fullNew);











